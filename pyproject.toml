[project]
name = "hearts"
version = "0.2.0"
description = "Hearts card game with Pygame UI"
requires-python = ">=3.10"
dependencies = [
    "pygame>=2.6",
    "Pillow>=10",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
